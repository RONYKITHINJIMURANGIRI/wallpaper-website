export default function ResolutionBadge({ label }) {
  return (
    <span className="inline-flex items-center rounded-full bg-white/10 px-2.5 py-1 text-[10px] font-black tracking-widest text-gray-200">
      {label}
    </span>
  );
}
